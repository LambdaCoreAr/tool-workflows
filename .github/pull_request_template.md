The pull request title is the release note and decides the version bump.

`feat:` minor · `fix:` patch · `!` or a `BREAKING CHANGE:` footer major ·
`docs: chore: refactor: test: ci: build: style: perf:` no release.
