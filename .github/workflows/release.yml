name: release

on:
  push:
    branches: [main]

jobs:
  release:
    permissions:
      contents: write
    uses: LambdaCoreAr/tool-workflows/.github/workflows/tool-release.yml@v1
    secrets: inherit
